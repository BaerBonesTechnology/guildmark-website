include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - build/**
    - .dart_tool/**
    - .dart_frog/**

linter:
  rules:
    public_member_api_docs: false   # Routes don't benefit from doc comments.
    no_default_cases: false        # Allow switch on HttpMethod to omit default case.
