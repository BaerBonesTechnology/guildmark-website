name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Build & Push Docker → Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build & push image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/guildmark-dashboard:latest
            ${{ secrets.DOCKER_USERNAME }}/guildmark-dashboard:${{ github.sha }}
          build-args: |
            VITE_API_URL=https://api.guildmark.co
            VITE_IS_LAUNCH=true
            VITE_IS_DEBUG=false

      - name: Deploy to server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            docker pull ${{ secrets.DOCKER_USERNAME }}/guildmark-dashboard:latest
            docker stop guildmark-dashboard || true
            docker rm   guildmark-dashboard || true
            docker run -d \
              --name guildmark-dashboard \
              --restart unless-stopped \
              -p 3000:80 \
              ${{ secrets.DOCKER_USERNAME }}/guildmark-dashboard:latest
